# Mock Optimizer

This optimizer is deterministic and intentionally simple.

Priority order:

1. Critical demand
2. Available route
3. Vehicle capacity

It produces a stable `solution.json` for demos and debugging.
